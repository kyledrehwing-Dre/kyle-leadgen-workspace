# 2026-03-28 - Leadgen Spec Lock Repair

## SPEC_VERSION
leadgen-canonical-v3

## Repair Summary
Reconciled live workflow governance into one canonical spec before any leadgen execution.

### Files patched
- AGENTS.md
- HEARTBEAT.md
- USER.md (workflow sections only)
- SOUL.md (workflow/boundaries only)
- TOOLS.md
- skills/linkedin_zoominfo_sop/SKILL.md
- skills/zoominfo_batch_enrichment_sop/SKILL.md
- scripts/validate_linkedin_zoominfo_sop.sh
- SOP-ZOOMINFO-MODIFIED.md (deprecated as non-canonical)

### Canonical decisions locked
- Canonical search universe = 13 exact ordered terms
- Browser rule = use the profile that verifiably attaches to Kyle's already logged-in Chrome session; do not assume profile name
- LinkedIn order = preferred verified LinkedIn/Sales Navigator path -> fallback A standard company people path -> fallback B company-page/associated-members browsing
- False zero != no people
- Company ambiguity is a blocker
- ZoomInfo order = Advanced Search -> Clear All -> Full Name -> Company -> widen confidence to All contacts / 50-100 -> fallback company page -> Employees -> Information Technology department
- Batch/export is optional accelerator only when exact row mapping is proven
- Tabs fixed to Daily Targets + Updated Format only
- Dedupe key fixed to LinkedIn URL
- Row identity fixed to LinkedIn URL first, then Company + Full Name verification
- Status values fixed to canonical sets in Daily Targets C / Updated Format J / Updated Format K
- Completion requires all 13 terms + paging stop rules + safe writes + current-run Phase 2 resolution + memory summary
- Continuation is autonomous; never ask permission to continue while valid work remains
- Execution ledger required after each company

### Drift removed
- Stale 10-term workflow wording
- Stale 11-term wording
- Hardcoded profile="user" / profile="chrome" assumptions
- Shallow-completion language
- Blank/Skipped ZoomInfo status drift from legacy notes

## Validation Results
- `bash scripts/validate_linkedin_zoominfo_sop.sh` -> PASS
- explicit drift scan over live workflow files -> PASS

## Skill Audit Summary
### Local relevant skills reviewed
- linkedin_zoominfo_sop -> retained and hardened
- zoominfo_batch_enrichment_sop -> retained and narrowed to optional Phase 2 accelerator
- gog -> relevant bundled Sheets skill; retained as tool guidance only

### ClawHub candidates reviewed
1. lead-hunter -> rejected (too broad, API/proxy heavy, CRM/outreach oriented, weak on exact row identity)
2. linkedin-lead-generation -> rejected (founder-report workflow, PDF/report bias, LinkedIn assumptions, not aligned to Sheets/ZoomInfo invariants)
3. data-enricher -> rejected (Notion/Hunter/Instagram workflow, wrong datastore and enrichment path)

## Execution Attempt Status
Blocked before live leadgen execution.

### Blocker
- Browser attach to Kyle's logged-in Chrome session is not yet available.
- `browser(status, profile=user)` failed because DevToolsActivePort was not available.
- `browser(start, profile=user)` reported existing-session attach timed out and indicated a browser attach approval prompt is needed.

### Exact next action
- Keep Chrome open and approve the existing-session browser attach prompt, then retry browser attach.
- Do not start LinkedIn, ZoomInfo, or Google Sheets execution until the user-session browser attach is verified.

## Resume Attempt - 2026-03-28 10:37 EDT
- Re-ran `bash scripts/validate_linkedin_zoominfo_sop.sh` -> PASS
- Re-checked browser profiles -> only `user` remains the candidate existing-session profile for Kyle's live Chrome session
- Re-tried `browser(status, profile=user)` -> failed; `DevToolsActivePort` still unavailable
- Re-tried `browser(start, profile=user)` -> failed again; existing-session attach timed out and still requested approval of the browser attach prompt

### Current stop reason
- Browser/session cannot yet be verified, so execution remains blocked at step 1 of the live run order.

## Resume Attempt - 2026-03-28 10:38 EDT
- Re-tried `browser(status, profile=user)` -> still failed; `DevToolsActivePort` unavailable
- Re-tried `browser(start, profile=user)` -> still failed; existing-session attach timed out and again requested approval of the browser attach prompt

### Current stop reason
- Browser/session still cannot be verified, so execution remains blocked at step 1 of the live run order.

## Deterministic Browser Option Test - 2026-03-28 10:51 EDT
Available tool-exposed browser options on host:
- `openclaw` -> driver `openclaw`, transport `cdp`, isolated/startable browser profile
- `user` -> driver `existing-session`, transport `chrome-mcp`, existing Chrome session attach path

Exact results:
- `browser(action=profiles, target=host)` -> returned `openclaw` and `user`
- `browser(action=status, target=host)` -> default `openclaw` profile available but not running
- `browser(action=start, target=host, profile=openclaw)` -> succeeded; isolated browser started
- `browser(action=status, target=host, profile=openclaw)` -> succeeded; isolated browser confirmed running
- `browser(action=status, target=host, profile=user)` -> failed; `DevToolsActivePort` unavailable for Chrome existing-session attach
- `browser(action=start, target=host, profile=user)` -> failed; attach timed out and explicitly requested approval of the browser attach prompt
- `browser(action=stop, target=host, profile=openclaw)` -> succeeded; isolated browser stopped after proving availability

### Current stop reason
- No browser profile has been verified as the canonical live session.
- The only existing-session attach path (`user`) remains blocked pending Chrome attach approval / live Chrome DevTools availability.

## Final Audit Note - 2026-03-28 11:14 EDT
- Added durable browser/session fallback language to live workflow files and validator:
  - try existing logged-in Chrome session if available
  - otherwise spawn a dedicated agent-owned browser session/profile
  - use the same verified session for the run
  - do not assume a profile name without verification
- Corrected a fixable live-sheet inconsistency in `Updated Format` rows 1416:1425 where columns `L/M/N` were shifted right.
  - Before fix: `L=""`, `M="2026-03-27"`, `N="MANUAL-20260327"`
  - After fix: `L="2026-03-27"`, `M="MANUAL-20260327"`, `N=""`
- Latest-run audit still found a historical mismatch between the 2026-03-27 memory summary and the current sheet state for `MANUAL-20260327`:
  - memory note says `Enriched (8)` / `Not Found (5)`
  - live sheet now shows `Enriched (11)` / `Not Found (2)`
- Historical memory was not rewritten; this note preserves the discrepancy for future repair or explanation.
